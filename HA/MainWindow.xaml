<Window x:Class="HA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        Title="Home Assistant Smart Controller" Height="850" Width="1400"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">

    <Window.Resources>
        <Style x:Key="DeviceButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl FontSize="15" Margin="5" Background="White" BorderThickness="0">
            <TabItem Header="🌐 Camera &amp; Web">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#F9F9F9" Padding="5" BorderThickness="0,0,0,1" BorderBrush="#DDD">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📍 Đang kết nối:" VerticalAlignment="Center" Margin="5,0,10,0" Foreground="#666" FontSize="12"/>
                            <TextBlock x:Name="lblCurrentUrl" Text="Chưa kết nối" VerticalAlignment="Center" FontWeight="Bold" Foreground="#0078D4" FontSize="12"/>
                        </StackPanel>
                    </Border>
                    <wv2:WebView2 Name="haWebView" Grid.Row="1" />
                </Grid>
            </TabItem>

            <TabItem Header="⚡ Điều Khiển Nhanh">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="icDeviceGroups" Margin="20">
                        <ItemsControl.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="GroupItem">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Expander Header="{Binding Name}" IsExpanded="True" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="#0078D4">
                                                        <ItemsPresenter />
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ItemsControl.GroupStyle>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Name}" 
                                        Tag="{Binding EntityId}" 
                                        Style="{StaticResource DeviceButtonStyle}"
                                        Click="BtnToggleDevice_Click"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="⚙️ Cài Đặt">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="CẤU HÌNH KẾT NỐI" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBox x:Name="txtUrlSetting" Tag="URL Home Assistant" Margin="0,0,0,10" Padding="5"/>
                        <TextBox x:Name="txtTokenSetting" Tag="Token" Margin="0,0,0,10" Height="60" TextWrapping="Wrap" Padding="5"/>
                        <Button Content="🚀 LƯU KẾT NỐI" Click="BtnSaveAndConnect_Click" Height="35" Background="#4CAF50" Margin="0,0,0,20"/>

                        <Separator Background="#DDD" Margin="0,10"/>

                        <TextBlock Text="THÊM THIẾT BỊ" FontSize="18" FontWeight="Bold" Margin="0,10,0,15"/>
                        <TextBlock Text="Tên thiết bị:"/>
                        <TextBox x:Name="txtNewName" Margin="0,0,0,10" Padding="5"/>
                        <TextBlock Text="Entity ID:"/>
                        <TextBox x:Name="txtNewId" Margin="0,0,0,10" Padding="5"/>
                        <TextBlock Text="Phòng/Nhóm:"/>
                        <TextBox x:Name="txtNewRoom" Margin="0,0,0,15" Padding="5" Text="Phòng Khách"/>
                        <Button Content="➕ THÊM MỚI" Click="BtnAddDevice_Click" Height="40" Background="#0078D4"/>
                    </StackPanel>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="DANH SÁCH THIẾT BỊ (Sửa trực tiếp trên bảng)" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                        <DataGrid Grid.Row="1" x:Name="dgDevices" AutoGenerateColumns="False" CanUserAddRows="False" 
                                  Background="White" RowHeight="35" FontSize="14">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Phòng" Binding="{Binding Room, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                                <DataGridTextColumn Header="Tên Hiển Thị" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                <DataGridTextColumn Header="Entity ID" Binding="{Binding EntityId, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                                <DataGridTemplateColumn Header="Thao tác" Width="100">
                                    <DataTemplate>
                                        <Button Content="Xóa" Background="#F44336" Click="BtnDeleteDevice_Click" Height="25" Padding="5,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>